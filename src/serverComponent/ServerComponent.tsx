import { FC } from 'react';

const ServerComponent: FC = () => {
  return (
    <div>
      <span>this component can called by server Component</span>
    </div>
  );
};

export default ServerComponent;
